[package]
name = "neonex-mockplatform"
version = "0.1.0"
edition = "2024"

[dependencies]
neonex-platform = { path = "../neonex-platform" }
cfg-if = "1.0.1"
bevy = {version = "0.16.1", features = ["webgl2"]}
serde_json = "1.0"
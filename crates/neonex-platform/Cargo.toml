[package]
name = "neonex-platform"
version = "0.1.0"
edition = "2024"

[features]

[dependencies]
cfg-if = "1.0.1"
serde = { version = "1.0", features = ["derive"]}
bevy = "0.16.1"
serde_json = "1.0"
neonex-macros = {path = "../neonex-macros"}